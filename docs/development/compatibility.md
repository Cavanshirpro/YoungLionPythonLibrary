# Compatibility

Stable target: CPython 3.10–3.14. Main desktop/server targets are Windows, Linux and macOS. Extra architectures/mobile/WASM are experimental and kept outside the normal PyPI bundle.

Wheels are platform/interpreter specific. Source builds require a matching local toolchain. Filesystem permissions, shortcuts, path casing and atomic replace behavior can differ by OS/filesystem.
