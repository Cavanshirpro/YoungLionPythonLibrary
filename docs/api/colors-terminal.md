# Colors and Terminal

`Colors` provides ANSI constants plus true-color RGB, 256-color, wrapping, stripping, foreground/background and gradient helpers.

`Terminal` provides capability detection, terminal size, ANSI-aware visible width, truncate/pad, table rendering, screen clear and textual progress bars without a TUI dependency.
